package mobile.core.business.pageObjects;

import mobile.core.driver.DriverManager;

public class MainMobilePage extends BaseMobilePage {

    public MainMobilePage(DriverManager driverManager) {
        super(driverManager);
    }
}
